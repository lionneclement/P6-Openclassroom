{% extends 'base.html.twig' %}

{% block title %}Modifier un style{% endblock %}

{% block body %}

<div class="pt-5">
    <div class="container pt-5">
        <h1 class="text-center pb-3">Modifier le style</h1>
        <div>
            {{ form_start(form) }}
                {{ form_row(form.name) }}
                <div class="text-center">
                    <button class="btn btn-secondary" type="submit">Valider</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
